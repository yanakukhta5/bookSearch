export type colors = {
    primary: string
    secondary: string
    third: string
    headerBg: string
    footerBg: string
    overlay: string
    backup: string
    mainBg: string
    transparent: '#00000000'
  }

export type colorTheme = {
  colors: colors
}